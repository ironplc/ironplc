set windows-shell := ["powershell.exe", "-c"]

alias ci := default
default: setup test

setup:
  pip install -r issue-resolver/requirements.txt pytest pytest-asyncio

test:
  cd issue-resolver && python -m pytest tests/ -v
